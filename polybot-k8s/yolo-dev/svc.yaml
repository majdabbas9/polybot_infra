apiVersion: v1
kind: Service
metadata:
  name: yolo-service
  namespace: dev
spec:
  selector:
    app: yolo-dev
  ports:
    - port: 8080         # Internal port (what Ingress sees)
      targetPort: 8080   # Your app's container port