version: '3.8'

services:
  nginx:
    image: nginx
    container_name: mynginx
    restart: always
    ports:
      - "443:443"
    volumes:
      - ${NGINX_CONF_DST}/default.conf:/etc/nginx/conf.d
      - ${CERTS_DIR}:/etc/nginx/ssl