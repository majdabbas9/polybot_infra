networks:
  monitoring_net:
    driver: bridge

services:
  grafana:
    image: grafana/grafana
    container_name: mygrafana
    restart: unless-stopped
    ports:
      - "3000:3000"
    networks:
      - monitoring_net


